<UserControl x:Class="HotelApp.Views.UserControl.SpecifyPriceControl"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            Width="600" Height="500">

    <!-- Тень вокруг окна -->
    <UserControl.Effect>
        <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="15" ShadowDepth="0"/>
    </UserControl.Effect>

    <Border CornerRadius="10">
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF222222" Offset="0" />
                    <GradientStop Color="#FF1E1E1E" Offset="0.5" />
                    <GradientStop Color="#FF1A1A1A" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>

            <!-- Заголовок и кнопка закрытия -->
            <Grid Grid.Row="0" Background="Black" Height="30" VerticalAlignment="Top" MouseDown="DragWindow_MouseDown">
                <TextBlock Text="Указать цены на комнаты" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0" Foreground="White" FontSize="16"/>
                <Button Content="X" Width="30" Height="30" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Foreground="White"
                    FontSize="14"
                    Click="CloseButton_Click"/>
            </Grid>

            <!-- Таблица номеров -->
            <DataGrid x:Name="RoomsDataGrid" 
                  SelectionMode="Single"
                  ItemsSource="{Binding Rooms}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="False" 
                  Margin="0,35,0,0" 
                  Background="#FF2A2A2A" 
                  Foreground="White" 
                  BorderBrush="#FF444444" 
                  BorderThickness="1">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF333333" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Padding" Value="10" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="#FF2A2A2A" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="Height" Value="35" />
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <!-- Название комнаты - только для чтения -->
                    <DataGridTextColumn Header="Комната" 
                            Binding="{Binding NameRoom}" 
                            Width="*" 
                            IsReadOnly="True"/>

                    <!-- Этаж комнаты - только для чтения -->
                    <DataGridTextColumn Header="Этаж" 
                        Binding="{Binding FloorRoom}" 
                        Width="*" 
                        IsReadOnly="True"/>

                    <!-- Цена - можно редактировать -->
                    <DataGridTextColumn Header="Цена" 
                            Binding="{Binding PriceRoom, UpdateSourceTrigger=PropertyChanged}" 
                            Width="*"
                            IsReadOnly="False"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Кнопка сохранения -->
            <Button Content="Применить изменения" 
                Height="35"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="10"
                Background="#0078D7"
                Foreground="White"
                FontSize="14"
                FontWeight="Bold"
                Click="SaveChangesButton_Click"/>

            <Canvas x:Name="WindowContainer" Grid.RowSpan="4" />
        </Grid>
    </Border>
</UserControl>
