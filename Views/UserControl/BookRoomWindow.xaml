<UserControl x:Class="HotelApp.Views.UserControl.BookRoomWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             Width="900" Height="600">
    <Border CornerRadius="8" Padding="10" Background="#FF1E1E1E" Margin="1">
        <Border.Effect>
            <DropShadowEffect Color="#FF222222" Opacity="0.5" BlurRadius="10" ShadowDepth="5" />
        </Border.Effect>

        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF222222" Offset="0" />
                    <GradientStop Color="#FF1E1E1E" Offset="0.5" />
                    <GradientStop Color="#FF1A1A1A" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>

            <!-- Верхняя панель -->
            <Grid Grid.Row="0" Background="Black" Height="30" VerticalAlignment="Top" MouseDown="DragWindow_MouseDown">
                <TextBlock Text="Бронирование номеров" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0" Foreground="White" FontSize="16"/>
                <Button Content="X" Width="30" Height="30" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Foreground="White"
                    FontSize="14"
                    Click="CloseButton_Click"/>
            </Grid>

            <!-- Основной контент -->
            <Grid Grid.Row="1" Margin="0,45,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <!-- Панель поиска -->
                    <RowDefinition Height="*" />
                    <!-- Таблица -->
                </Grid.RowDefinitions>

                <!-- Панель поиска -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,10">
                    <TextBlock Text="Дата заезда:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="White" FontSize="14" />
                    <DatePicker Name="StartDatePicker" SelectedDate="{x:Static sys:DateTime.Now}" Width="120" Background="White" Foreground="Black" />
                    <TextBlock Text="Дата выезда:" VerticalAlignment="Center" Margin="10,0,5,0" Foreground="White" FontSize="14" />
                    <DatePicker Name="EndDatePicker" SelectedDate="{x:Static sys:DateTime.Now}" Width="120" Background="White" Foreground="Black" />
                    <Button Content="Найти свободные номера" Click="FindAvailableRooms_Click" Margin="10,0,0,0" Background="#FF4CAF50" Foreground="White" FontSize="14" />
                    <Button Content="Сбросить фильтр" Click="ResetFilter_Click" Margin="10,0,0,0" Background="#FF03A9F4" Foreground="White" FontSize="14" />
                    <Button Content="Забронировать" Click="Book_Click" Margin="10,0,0,0" Background="#FFF44503" Foreground="White" FontSize="14" />
                </StackPanel>

                <!-- Таблица номеров -->
                <DataGrid Grid.Row="1" x:Name="RoomsDataGrid" 
                          SelectionMode="Single"
                          ItemsSource="{Binding Rooms}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True" 
                          Margin="0,10,0,0" 
                          Background="#FF2A2A2A" 
                          Foreground="White" 
                          BorderBrush="#FF444444" 
                          BorderThickness="1">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#FF333333" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="FontWeight" Value="Bold" />
                            <Setter Property="Padding" Value="10" />
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="#FF2A2A2A" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="FontSize" Value="14" />
                            <Setter Property="Height" Value="35" />
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Этаж" Binding="{Binding FloorRoom}" Width="*" />
                        <DataGridTextColumn Header="Номер" Binding="{Binding NumberRoom}" Width="*" />
                        <DataGridTextColumn Header="Категория" Binding="{Binding TypeRoom}" Width="*" />
                        <DataGridTextColumn Header="Клиент" Binding="{Binding Client}" Width="*" />
                        <DataGridTextColumn Header="Статус" Binding="{Binding StatusRoom}" Width="*" />
                        <DataGridTextColumn Header="Дата заезда" Binding="{Binding CheckInDate}" Width="*" />
                        <DataGridTextColumn Header="Дата выезда" Binding="{Binding CheckOutDate}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Canvas x:Name="WindowContainer" Grid.RowSpan="2" />
        </Grid>
    </Border>
</UserControl>